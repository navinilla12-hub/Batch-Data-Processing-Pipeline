# Batch-Data-Processing-Pipeline
Built a batch data processing pipeline using Python and SQL concepts to ingest, clean, transform, and validate structured datasets. Implemented data quality checks and generated analytics-ready outputs. Simulates real-world ETL workflows and production-style data pipeline processing.
